head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2011.03.28.09.09.12;	author zhang-tengyu;	state Exp;
branches;
next	;
deltatype	text;
kopt	b;
permissions	666;
commitid	5644d9050285d38;
filename	xbruito_account_triggers.sql;


desc
@@


1.1
log
@*** empty log message ***
@
text
@------------------------------------------------------------------
-- Autogenerated per-table triggers for mutators
------------------------------------------------------------------

--------------------------------------------------
--- ruito_fin_transaction

create trigger inv_ruito_fin_transaction_ins
  after insert on ruito_fin_transaction
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_fin_transaction', 'ins', :new.account_id, :new.fin_transaction_id );
  end;
/
show errors

create trigger inv_ruito_fin_transaction_upd
  before update on ruito_fin_transaction
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_fin_transaction', 'upd', :old.account_id, :old.fin_transaction_id );
  end;
/
show errors

create trigger inv_ruito_fin_transaction_del
  before delete on ruito_fin_transaction
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_fin_transaction', 'del', :old.account_id, :old.fin_transaction_id );
  end;
/
show errors

--------------------------------------------------
--- ruito_order_execution

create trigger inv_ruito_order_execution_ins
  after insert on ruito_order_execution
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_execution', 'ins', :new.account_id, :new.order_execution_id );
  end;
/
show errors

create trigger inv_ruito_order_execution_upd
  before update on ruito_order_execution
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_execution', 'upd', :old.account_id, :old.order_execution_id );
  end;
/
show errors

create trigger inv_ruito_order_execution_del
  before delete on ruito_order_execution
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_execution', 'del', :old.account_id, :old.order_execution_id );
  end;
/
show errors

--------------------------------------------------
--- ruito_order

create trigger inv_ruito_order_ins
  after insert on ruito_order
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order', 'ins', :new.account_id, :new.order_id );
  end;
/
show errors

create trigger inv_ruito_order_upd
  before update on ruito_order
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order', 'upd', :old.account_id, :old.order_id );
  end;
/
show errors

create trigger inv_ruito_order_del
  before delete on ruito_order
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order', 'del', :old.account_id, :old.order_id );
  end;
/
show errors

--------------------------------------------------
--- ruito_order_unit

create trigger inv_ruito_order_unit_ins
  after insert on ruito_order_unit
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_unit', 'ins', :new.account_id, :new.order_unit_id );
  end;
/
show errors

create trigger inv_ruito_order_unit_upd
  before update on ruito_order_unit
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_unit', 'upd', :old.account_id, :old.order_unit_id );
  end;
/
show errors

create trigger inv_ruito_order_unit_del
  before delete on ruito_order_unit
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_unit', 'del', :old.account_id, :old.order_unit_id );
  end;
/
show errors

--------------------------------------------------
--- ruito_order_action

create trigger inv_ruito_order_action_ins
  after insert on ruito_order_action
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_action', 'ins', :new.account_id, :new.order_action_id );
  end;
/
show errors

create trigger inv_ruito_order_action_upd
  before update on ruito_order_action
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_action', 'upd', :old.account_id, :old.order_action_id );
  end;
/
show errors

create trigger inv_ruito_order_action_del
  before delete on ruito_order_action
  for each row
  declare
  begin
    invalidation.on_mutate( 'ruito_order_action', 'del', :old.account_id, :old.order_action_id );
  end;
/
show errors

@
